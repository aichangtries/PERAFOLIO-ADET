import 'package:flutter/material.dart';
import 'core/theme/app_colors.dart';
import 'core/theme/app_theme.dart';
import 'data/mock_data.dart';
import 'models/bank.dart';

const purple = AppColors.purple, purpleDark = AppColors.purpleDark, bg = AppColors.background, surface = AppColors.surface, elevated = AppColors.elevated, ink = AppColors.ink, muted = AppColors.muted, line = AppColors.line, green = AppColors.success, error = AppColors.error;

void main() => runApp(const PeraFolioApp());

class PeraFolioApp extends StatelessWidget { const PeraFolioApp({super.key}); @override Widget build(BuildContext context) => MaterialApp(debugShowCheckedModeBanner:false, theme: AppTheme.dark, home:const Shell()); }
class Shell extends StatefulWidget { const Shell({super.key}); @override State<Shell> createState()=>_ShellState(); }
class _ShellState extends State<Shell> { final banks=MockData.banks(); final tx=MockData.transactions(); int tab=0; String screen='home', source='GCash', destination='Maya'; String amount='2000';
List<Bank> get connected=>banks.where((b)=>b.connected).toList(); void go(String s)=>setState(()=>screen=s); void addTx(String kind){setState((){tx.insert(0,Tx(kind=='pay'?'Coffee Shop':destination,'$source • ${kind=='pay'?'Payment':'Transfer'}',kind=='pay'?'-₱500.00':'-₱$amount'));screen='home';tab=0;});}
@override Widget build(BuildContext context){if(screen=='welcome')return Welcome(onStart:()=>go('connect')); if(screen=='connect')return Connect(banks:banks,onDone:()=>go('home'),onVerify:(b)=>setState(()=>b.connected=true)); if(screen=='transfer')return Transfer(banks:connected,source:source,destination:destination,amount:amount,onSource:(v)=>setState(()=>source=v),onDestination:(v)=>setState(()=>destination=v),onAmount:(v)=>amount=v,onBack:()=>go('home'),onReview:()=>go('transferReview')); if(screen=='transferReview')return Review(title:'Review Transfer',source:source,destination:destination,amount:amount,onBack:()=>go('transfer'),onConfirm:()=>go('transferSuccess')); if(screen=='transferSuccess')return Success(title:'Transfer Successful',amount:'₱$amount',onDone:()=>addTx('transfer')); if(screen=='pay')return Pay(banks:connected,source:source,onSource:(v)=>setState(()=>source=v),onBack:()=>go('home'),onReview:()=>go('payReview')); if(screen=='payReview')return Review(title:'Review Payment',source:source,destination:'Coffee Shop',amount:'500',onBack:()=>go('pay'),onConfirm:()=>go('paySuccess')); if(screen=='paySuccess')return Success(title:'Payment Successful',amount:'₱500.00',onDone:()=>addTx('pay')); return Scaffold(appBar:AppBar(title:Text(tab==0?'Good evening, Alessandra':tab==1?'Your Accounts':tab==2?'Transactions':'Profile & Settings'),actions:[IconButton(onPressed:()=>setState(()=>tab=3),icon:const Icon(Icons.person_outline))]),body:tab==0?Dashboard(banks:connected,tx:tx,onTransfer:()=>go('transfer'),onPay:()=>go('pay')):tab==1?Accounts(banks:connected):tab==2?Transactions(tx:tx):Profile(onDone:()=>setState(()=>tab=0)),bottomNavigationBar:NavigationBar(selectedIndex:tab==3?0:tab,onDestinationSelected:(i)=>setState(()=>tab=i),destinations:const[NavigationDestination(icon:Icon(Icons.home_outlined),selectedIcon:Icon(Icons.home),label:'Home'),NavigationDestination(icon:Icon(Icons.account_balance_wallet_outlined),label:'Accounts'),NavigationDestination(icon:Icon(Icons.list_alt_outlined),label:'Activity')]);}}
class Welcome extends StatelessWidget{final VoidCallback onStart;const Welcome({super.key,required this.onStart});@override Widget build(BuildContext c)=>Scaffold(body:Center(child:Padding(padding:const EdgeInsets.all(24),child:Column(mainAxisAlignment:MainAxisAlignment.center,children:[const Icon(Icons.account_balance_wallet,size:52,color:purple),const SizedBox(height:18),const Text('PeraFolio',style:TextStyle(fontSize:28,fontWeight:FontWeight.bold)),const SizedBox(height:28),const Text('All your money.\nOne place.',textAlign:TextAlign.center,style:TextStyle(fontSize:26,fontWeight:FontWeight.bold)),const SizedBox(height:16),const Text('Connect your banks and e-wallets to see your complete financial picture in one application.',textAlign:TextAlign.center,style:TextStyle(color:muted)),const SizedBox(height:30),SizedBox(width:double.infinity,child:FilledButton(onPressed:onStart,child:const Text('Get Started')))])));}
class Connect extends StatelessWidget{final List<Bank> banks;final VoidCallback onDone;final ValueChanged<Bank> onVerify;const Connect({super.key,required this.banks,required this.onDone,required this.onVerify});void verify(BuildContext c,Bank b){final input=TextEditingController();showDialog(context:c,builder:(_)=>AlertDialog(title:Text('Verify ${b.name}'),content:TextField(controller:input,maxLength:6,keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'6-digit verification code')),actions:[TextButton(onPressed:()=>Navigator.pop(c),child:const Text('Cancel')),FilledButton(onPressed:()=>input.text.length==6?{onVerify(b),Navigator.pop(c)}:null,child:const Text('Verify'))]));}@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('Connect Your Accounts')),body:ListView(padding:const EdgeInsets.all(20),children:[const Text('Bring your finances together in one place.',style:TextStyle(color:muted)),...banks.map((b)=>Card(child:ListTile(leading:const Icon(Icons.account_balance_wallet),title:Text(b.name),subtitle:Text(b.type),trailing:b.connected?const Text('Connected',style:TextStyle(color:green)):FilledButton(onPressed:()=>verify(c,b),child:const Text('Connect'))))),FilledButton(onPressed:onDone,child:const Text('Continue'))]));}}
class Dashboard extends StatefulWidget{final List<Bank>banks;final List<Tx>tx;final VoidCallback onTransfer,onPay;const Dashboard({super.key,required this.banks,required this.tx,required this.onTransfer,required this.onPay});@override State<Dashboard>createState()=>_DashboardState();}class _DashboardState extends State<Dashboard>{String period='This Month',category='All';@override Widget build(BuildContext c){final heights=period=='Last Month'?[.4,.7,.5,.8,.6]:period=='This Year'?[.8,.5,.9,.6,.85]:[.3,.5,.4,.75,.55];final shown=category=='All'?widget.tx:widget.tx.where((t)=>t.detail.toLowerCase().contains(category.toLowerCase())).toList();return ListView(padding:const EdgeInsets.all(20),children:[const Text('Total Balance',style:TextStyle(color:muted)),const Text('₱45,680.00',style:TextStyle(fontSize:32,fontWeight:FontWeight.bold)),Text('Across ${widget.banks.length} connected accounts',style:const TextStyle(color:muted)),const SizedBox(height:20),Row(children:[Expanded(child:FilledButton(onPressed:widget.onTransfer,child:const Text('Transfer'))),const SizedBox(width:10),Expanded(child:FilledButton(onPressed:widget.onPay,child:const Text('Pay')))]),const SizedBox(height:24),Row(mainAxisAlignment:MainAxisAlignment.spaceBetween,children:[const Text('Spending Overview',style:TextStyle(fontWeight:FontWeight.bold)),DropdownButton(value:period,items:['This Month','Last Month','This Year'].map((x)=>DropdownMenuItem(value:x,child:Text(x))).toList(),onChanged:(x)=>setState(()=>period=x!))]),SizedBox(height:130,child:Row(crossAxisAlignment:CrossAxisAlignment.end,mainAxisAlignment:MainAxisAlignment.spaceEvenly,children:heights.map((h)=>Container(width:28,height:100*h,color:purple)).toList())),SingleChildScrollView(scrollDirection:Axis.horizontal,child:Row(children:['All','Food & Dining','Income','Bills'].map((x)=>Padding(padding:const EdgeInsets.only(right:8),child:ChoiceChip(label:Text(x),selected:category==x,onSelected:(_)=>setState(()=>category=x)))).toList())),const SizedBox(height:20),const Text('Recent Transactions',style:TextStyle(fontWeight:FontWeight.bold)),...shown.map((t)=>ListTile(title:Text(t.name),subtitle:Text(t.detail,style:const TextStyle(color:muted)),trailing:Text(t.amount,style:TextStyle(color:t.amount.startsWith('+')?green:null))) ]);}}
class Accounts extends StatelessWidget{final List<Bank>banks;const Accounts({super.key,required this.banks});@override Widget build(BuildContext c)=>ListView(padding:const EdgeInsets.all(20),children:[const Text('Your Accounts',style:TextStyle(fontSize:22,fontWeight:FontWeight.bold)),...banks.map((b)=>Card(child:ListTile(title:Text(b.name),subtitle:Text(b.type),trailing:Text(b.balance))))]);}
class Transactions extends StatelessWidget{final List<Tx>tx;const Transactions({super.key,required this.tx});@override Widget build(BuildContext c)=>ListView(padding:const EdgeInsets.all(20),children:tx.map((t)=>ListTile(title:Text(t.name),subtitle:Text(t.detail,style:const TextStyle(color:muted)),trailing:Text(t.amount))).toList());}
class Profile extends StatelessWidget{final VoidCallback onDone;const Profile({super.key,required this.onDone});@override Widget build(BuildContext c)=>ListView(padding:const EdgeInsets.all(20),children:[Row(mainAxisAlignment:MainAxisAlignment.spaceBetween,children:[const Text('Profile & Settings',style:TextStyle(fontSize:22,fontWeight:FontWeight.bold)),TextButton(onPressed:onDone,child:const Text('Done'))]),...['Personal details','Notifications','Security','Help & support'].map((x)=>ListTile(title:Text(x),trailing:const Icon(Icons.chevron_right))) ]);}
class Transfer extends StatelessWidget{final List<Bank>banks;final String source,destination,amount;final ValueChanged<String>onSource,onDestination,onAmount;final VoidCallback onBack,onReview;const Transfer({super.key,required this.banks,required this.source,required this.destination,required this.amount,required this.onSource,required this.onDestination,required this.onAmount,required this.onBack,required this.onReview});@override Widget build(BuildContext c){final targets=banks.where((b)=>b.name!=source).toList();return Scaffold(appBar:AppBar(title:const Text('Transfer Money'),leading:IconButton(onPressed:onBack,icon:const Icon(Icons.arrow_back))),body:ListView(padding:const EdgeInsets.all(20),children:[DropdownButtonFormField(value:source,decoration:const InputDecoration(labelText:'FROM'),items:banks.map((b)=>DropdownMenuItem(value:b.name,child:Text(b.name))).toList(),onChanged:(x)=>onSource(x!)),const SizedBox(height:14),DropdownButtonFormField(value:targets.any((b)=>b.name==destination)?destination:targets.first.name,decoration:const InputDecoration(labelText:'TO'),items:targets.map((b)=>DropdownMenuItem(value:b.name,child:Text(b.name))).toList(),onChanged:(x)=>onDestination(x!)),const SizedBox(height:20),TextField(keyboardType:TextInputType.number,controller:TextEditingController(text:amount),onChanged:onAmount,decoration:const InputDecoration(labelText:'Transfer amount',prefixText:'₱ ')),const SizedBox(height:20),FilledButton(onPressed:onReview,child:const Text('Review Transfer'))]));}}
class Pay extends StatelessWidget{final List<Bank>banks;final String source;final ValueChanged<String>onSource;final VoidCallback onBack,onReview;const Pay({super.key,required this.banks,required this.source,required this.onSource,required this.onBack,required this.onReview});@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('Pay'),leading:IconButton(onPressed:onBack,icon:const Icon(Icons.arrow_back))),body:ListView(padding:const EdgeInsets.all(20),children:[Container(height:180,color:surface,child:const Center(child:Icon(Icons.qr_code_scanner,size:70,color:muted))),const SizedBox(height:18),DropdownButtonFormField(value:source,decoration:const InputDecoration(labelText:'Payment Source'),items:banks.map((b)=>DropdownMenuItem(value:b.name,child:Text('${b.name} • ${b.balance}'))).toList(),onChanged:(x)=>onSource(x!)),const SizedBox(height:20),FilledButton(onPressed:onReview,child:const Text('Scan to Pay'))]));}}
class Review extends StatelessWidget{final String title,source,destination,amount;final VoidCallback onBack,onConfirm;const Review({super.key,required this.title,required this.source,required this.destination,required this.amount,required this.onBack,required this.onConfirm});@override Widget build(BuildContext c){final payment=title.contains('Payment');return Scaffold(appBar:AppBar(title:Text(title,style:const TextStyle(fontSize:24,fontWeight:FontWeight.bold)),leading:IconButton(onPressed:onBack,icon:const Icon(Icons.arrow_back))),body:ListView(padding:const EdgeInsets.all(24),children:[Center(child:Column(children:[CircleAvatar(radius:28,backgroundColor:elevated,child:Icon(payment?Icons.local_cafe:Icons.swap_horiz,color:muted)),const SizedBox(height:12),Text(destination,style:const TextStyle(fontSize:20,fontWeight:FontWeight.bold)),if(payment)const Text('Merchant ID: CS-98213',style:TextStyle(color:muted,fontSize:12)),const SizedBox(height:8),Text('₱$amount',style:const TextStyle(fontSize:32,fontWeight:FontWeight.bold))])),const SizedBox(height:28),Text(payment?'PAY WITH':'TRANSFER FROM',style:const TextStyle(color:muted,fontSize:12)),const SizedBox(height:10),Card(child:ListTile(leading:const CircleAvatar(child:Text('G')),title:Text(source),subtitle:const Text('Balance: ₱12,450'),trailing:const Icon(Icons.check_circle))),Card(child:ListTile(title:Text(payment?'Maya':'To account'),subtitle:Text(payment?'Balance: ₱8,230':destination))),Card(child:Padding(padding:const EdgeInsets.all(16),child:Column(children:[_line('Amount','₱$amount'),_line('Fee',payment?'₱0.00':'₱15.00'),const Divider(),_line('Total charged',payment?'₱$amount':'₱${amount}')] ))),const SizedBox(height:18),SizedBox(width:double.infinity,child:FilledButton(onPressed:onConfirm,child:Text(payment?'Confirm Payment':'Confirm Transfer'))),TextButton(onPressed:onBack,child:const Text('Cancel'))]));}}
Widget _line(String a,String b)=>Padding(padding:const EdgeInsets.symmetric(vertical:6),child:Row(mainAxisAlignment:MainAxisAlignment.spaceBetween,children:[Text(a,style:const TextStyle(color:muted)),Text(b,style:const TextStyle(fontWeight:FontWeight.bold))]));
class Success extends StatelessWidget{final String title,amount;final VoidCallback onDone;const Success({super.key,required this.title,required this.amount,required this.onDone});@override Widget build(BuildContext c)=>Scaffold(body:SafeArea(child:ListView(padding:const EdgeInsets.all(24),children:[const SizedBox(height:24),const Center(child:CircleAvatar(radius:43,backgroundColor:surface,child:Icon(Icons.check,size:34,color:ink))),const SizedBox(height:42),Center(child:Text(title,style:const TextStyle(fontSize:24,fontWeight:FontWeight.bold))),Center(child:Text(amount,style:const TextStyle(fontSize:32,fontWeight:FontWeight.bold))),const SizedBox(height:30),Card(child:Padding(padding:const EdgeInsets.all(18),child:Column(children:[_line(title.contains('Payment')?'PAID TO':'TRANSFERRED TO',title.contains('Payment')?'Coffee Shop':'Maya'),_line(title.contains('Payment')?'PAID FROM':'TRANSFERRED FROM','GCash'),_line('DATE & TIME','20 Oct 2026, 14:30'),_line('REFERENCE','TXN-00012345')] ))),const SizedBox(height:28),Card(child:ListTile(leading:const CircleAvatar(child:Text('G')),title:const Text('GCash Balance'),trailing:Text(title.contains('Payment')?'₱11,950':'₱10,435',style:const TextStyle(fontWeight:FontWeight.bold)))),const SizedBox(height:22),SizedBox(width:double.infinity,child:FilledButton(onPressed:onDone,child:const Text('Done')))])));}}

// This reference app uses in-memory mock data to mirror the web prototype without requiring a backend.
